
# 1. 의존성 (Dependencies)
# 루트 및 모든 서브 폴더의 node_modules 폴더 제외
**/node_modules
# 로그 파일 및 캐시 제외
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 2. 빌드 및 배포 결과물 (Build Outputs)
# 프론트엔드 빌드 결과물 (Vercel에서 빌드)
digimon-tamagotchi-frontend/build

# 백엔드 빌드 결과물 (Express/Serverless 함수로 사용될 수 있는 경우)
backend/dist
backend/build
backend/build/

# 3. 보안 및 설정 파일 (Security & Configuration)
# 환경 변수 제외 (Vercel 대시보드에서 관리)
.env
.env.local
.env.*.local
.env.development

# 4. OS 관련 파일
.DS_Store
digimontama_1/
