# 스탯 센터 수명 표기 디자인 QA

## 비교 대상

- 소스 시각 정본: `/var/folders/c3/yvqftb655357f3vlx335f7lm0000gn/T/codex-clipboard-95990a51-4ee5-460e-a42e-a63fee02d223.png` (746×914px)
- 브라우저 렌더링 구현: `/private/tmp/stats-center-life-duration-2026-08-11.png` (420×676px)
- 렌더링: Codex 인앱 브라우저, `http://localhost:3001`, CSS viewport 420×676, device scale 1
- 상태: 운영자 `[고급·진단]` 탭의 `주기·타이머` 섹션, 수명 `1일 11시간 17분 4초`

## 밀도·상태 정규화

- 소스는 약 2배 밀도의 모달 부분 캡처로 보고 약 373 CSS px 폭으로 정규화했다. 구현 모달은 420 CSS px viewport 안에서 약 340 CSS px 콘텐츠 폭을 사용한다.
- 두 화면 모두 밝은 테마, 운영자 진단 탭, 같은 주기·남은 시간 값을 사용했다. 비교는 전체 모달 비율과 수명 행의 줄바꿈·정렬을 함께 확인했다.

## 전체 화면 및 핵심 부분 비교

- 기존 탭, 내부 스크롤, 행 구분선, 좌측 라벨·우측 값 구조가 유지됐다.
- 주기와 남은 시간은 기존 `분·초` 표기를 유지하고, 누적 수명만 `일·시간·분·초` 형식으로 변경됐다.
- `1일 11시간 17분 4초`가 420px viewport에서 한 줄로 표시되며 오른쪽 정렬, 행 높이와 다음 행 간격이 깨지지 않는다.
- 소스와 구현 캡처를 한 입력에서 함께 열어 글꼴 크기, 행 리듬, 값 너비와 스크롤 상태를 비교했다.

## 필수 충실도 표면

- **글꼴·타이포그래피:** 기존 system sans와 숫자 tabular 정렬을 유지했다. 긴 수명 값에 줄바꿈·잘림이 없다.
- **간격·레이아웃 리듬:** 기존 행 높이, 좌우 패딩, 섹션 간격과 스크롤 폭을 유지했다.
- **색상·토큰:** 회색 행 배경·테두리, 파란 활성 탭과 흰 모달 surface를 변경하지 않았다.
- **이미지·자산:** 텍스트 포맷 변경으로 신규 이미지나 자산이 없다.
- **문구·콘텐츠:** 누적 수명은 `N일 N시간 N분 N초`, 나머지 타이머는 `N분 N초`라는 역할별 표기 규칙이 화면에 일관되게 반영됐다.

## Findings

- P0/P1/P2 시각 불일치나 기능 차단 없음.

## Open Questions

- 없음. 실제 컴포넌트 캡처에 사용한 임시 QA 라우트는 제거했고 `App.jsx`에는 변경이 남지 않았다.

## Implementation Checklist

- [x] 수명 `일·시간·분·초` 변환
- [x] 다른 주기·타이머의 `분·초` 계약 유지
- [x] 420×676 좁은 화면에서 한 줄 표시 확인
- [x] 진단 탭 선택과 내부 스크롤 확인
- [x] 새 QA 탭 console error/warning 0건 확인

## Comparison History

- 1차 동시 비교에서 작업 범위의 P0/P1/P2 발견은 없었다.
- 비교 후 시각 수정이 필요하지 않아 post-fix 반복 비교는 없었다.

## Follow-up Polish

- 없음.

final result: passed
