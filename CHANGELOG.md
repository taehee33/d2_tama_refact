# 변경 기록

이 문서는 운영 배포 버전별 사용자 영향 변경사항을 기록합니다.

## [0.8.0.0] - 2026-09-01

### Added

- 케어미스 발생·해소·진화·환생을 revision과 receipt chain으로 검증하는 V2 저장 구조를 추가했습니다.
- 같은 command를 반복 전송해도 결과가 한 번만 반영되며, stale revision은 관련 문서를 변경하지 않고 거부합니다.
- 삭제 operation·slotId lock·5분 execution lease를 사용하는 trusted V2 슬롯 삭제를 추가했습니다.

### Changed

- V2 슬롯의 command, native init, integrity 조회와 삭제를 인증된 서버 API가 처리하고, migration·baseline override·linked-head repair는 operator 권한으로 분리했습니다.
- V2 저장이 동시에 발생하면 단일 queue에서 최신 revision과 receipt를 이어받아 순차적으로 처리합니다.
- IndexedDB outbox는 trusted 삭제가 완료된 뒤에만 해당 slot instance 대기 작업을 정리하며, 진행 중이거나 실패하면 재시도를 위해 보존합니다.

### Security

- Firestore Rules가 V2 slot·receipt·incident·deletion operation의 클라이언트 직접 쓰기를 거부하고, 삭제 lock이 있는 동안 같은 slotId의 재사용과 descendant 쓰기를 차단합니다.
- 일반 사용자는 자신의 슬롯만 삭제할 수 있으며 action이나 target UID를 변조해 operator 기능으로 승격할 수 없습니다.

## [0.7.2.0] - 2026-08-29

### Added

- 디지몬 상태와 기존 스탯 패널에서 현재 진화 구간의 수면 방해 횟수를 확인할 수 있습니다.
- 신규 디지몬 상태에서 수면 방해 행을 펼쳐 발생 사유와 시각을 최신순으로 확인할 수 있습니다.

### Changed

- 상세 기록이 카운트보다 적거나 진화 단계 시작 시각이 없는 기존 슬롯은 보관 한도·레거시 범위 안내와 함께 안전하게 표시합니다.
- 신규·기존 스탯 화면이 같은 수면 방해 로그 필터를 사용해 진화 구간과 정렬 기준을 일치시킵니다.

## [0.7.1.0] - 2026-08-28

### Changed

- 기존 슬롯의 케어미스 기록이 일부만 남아 있을 때 현재 단계의 발생·해소 이력을 먼저 확인하고 부족한 기록만 안전하게 복구합니다.
- 다른 기기가 케어미스 기록을 확인 중이면 lease 만료 후 슬롯 로드를 자동으로 다시 시도합니다.
- 몰입형 셸 선택에서 `민트`와 `클리어 블루`를 제거하고 기존 저장값은 기본 스킨으로 안전하게 전환합니다.
- 픽셀 세로 스킨의 실제 게임 화면 위치를 기기 프레임에 맞게 조정했습니다.

### Fixed

- 오래된 `ambiguous` 또는 `in_progress` 상태 문자열 때문에 정상 슬롯의 게임 진입이 계속 차단될 수 있던 문제를 수정했습니다.
- 케어미스 복구 시 현재 단계보다 이전이거나 미래인 시각, 서로 충돌하는 identity·카운터 및 순서가 맞지 않는 사건을 임의로 저장하지 않도록 보강했습니다.
- 복구한 케어미스 기록을 실제 발생 시각으로 오인하지 않도록 `복구된 기록 · 실제 시각 알 수 없음`으로 표시합니다.

## [0.7.0.0] - 2026-08-28

### Added

- 케어미스 발생 이력을 현재 디지몬 생명과 진화 단계에 맞춰 복구해, 앱을 다시 열거나 연결이 끊겨도 실제 호출 횟수와 미해결 케어미스를 잃지 않게 했습니다.
- 케어미스 복구를 확인할 수 있을 때만 게임을 다시 열고, 확인할 수 없는 상태에서는 저장된 수치를 임의로 덮어쓰지 않는 안전한 복구 흐름을 추가했습니다.

### Changed

- 케어미스 발생·해소·진화·환생·냉장고 이동을 순서가 보장되는 저장 흐름으로 처리해 반복 전송에도 결과가 한 번만 반영되도록 했습니다.
- 알림함은 최신 표시 가능 알림만 제한적으로 읽고 중복 요청을 재사용해, 오래된 알림이 많아져도 화면 진입 비용이 커지지 않게 했습니다.
- 홈과 플레이 허브에서 최근 디지몬을 가로형 이어하기 카드로 먼저 보여주고, 모바일 화면에 맞게 슬롯 목록과 빠른 이동 정보를 압축했습니다.
- 게임 화면 크기 입력을 편집하기 쉽게 정리하고, 스파링 상대 카드에서 버전별 디지몬 이미지와 별명을 확인할 수 있게 했습니다.
- 운영 환경에서 저장·조회 정합성과 Firestore 읽기 비용을 확인할 수 있도록 검증 기준과 측정 자료를 정리했습니다.

### Fixed

- 새 슬롯의 빈 스탯을 복구 오류로 잘못 판정해 게임 진입을 막거나 허위 수면 케어미스를 만들던 문제를 수정했습니다.
- 진화 전 케어미스 기록이 현재 단계의 호출 이력과 미확인 배지에 섞여 표시되던 문제를 수정했습니다.

## [0.6.0.0] - 2026-08-24

### Changed

- 모바일 홈과 플레이 허브에서 최근 디지몬을 가로형 이어하기 카드로 먼저 확인하고 바로 게임을 이어갈 수 있게 했습니다.
- 홈 요약·빠른 이동·다른 디지몬 목록과 플레이 허브의 슬롯 목록을 모바일 화면에 맞게 압축했습니다.
- 저장된 슬롯 보기 설정을 계속 우선 적용하면서, 모바일에는 `간략히`, 데스크톱에는 기존 `자세히` 기본값을 적용했습니다.
- 320px 폭에서도 상단 브랜드·채팅 표시와 고정 내비게이션이 잘리지 않도록 모바일 프레임과 safe-area 여백을 조정했습니다.

## [0.5.10.0] - 2026-08-24

### Added

- 알림 센터가 로그인한 사용자의 최신 표시 가능 알림을 최대 10개까지 안정적인 순서로 불러옵니다.

### Changed

- 알림 센터의 중복 요청을 60초 동안 재사용하고 동시에 발생한 요청은 한 번의 네트워크 호출로 공유합니다.
- 설정·진단용 알림 상태 조회와 전역 알림함 조회를 분리해 누적 알림 수가 늘어도 일반 화면의 읽기 비용이 증가하지 않도록 개선했습니다.
- 읽음 처리는 현재 표시된 알림 ID 최대 10개만 갱신하며 읽음 후에도 알림함 표시 상태를 유지합니다.

### Fixed

- 알림함을 열 때마다 과거 알림 컬렉션 전체를 다시 읽던 문제를 제거했습니다.
- 숫자 `0`으로 기록된 유효한 읽음 시각도 읽은 상태로 판정하도록 보완했습니다.

## [0.5.9.0] - 2026-08-22

### Added

- 스파링 슬롯 선택 카드에서 상대 디지몬의 버전별 이미지와 별명을 확인할 수 있게 했습니다.

### Changed

- 게임 화면 기본 크기를 `250×250px`로 변경했습니다.
- 화면 크기 숫자 입력 중에는 빈 값과 중간 숫자를 자유롭게 편집하고, 입력 완료 시에만 `100~600px` 범위로 보정합니다.

### Fixed

- 기존 화면 크기 저장값을 최초 1회 `250×250px`으로 전환하되, 이후 사용자가 저장한 크기는 유지하도록 했습니다.

## [0.5.8.0] - 2026-08-22

### Changed

- 모바일 Ghost 아레나의 현재 디지몬 이름을 한글로 표시하고 긴 이름도 최대 두 줄로 확인할 수 있게 했습니다.
- 현재 디지몬 전적·등록 상태·Ghost 등록 버튼을 항상 표시하고 불필요한 펼치기·접기 조작을 제거했습니다.
- 도전 상대 목록이 남은 높이를 사용해 독립적으로 스크롤되도록 카드와 Power 배지의 모바일 밀도를 조정했습니다.

### Fixed

- Ghost 아레나를 연 상태에서 뒤 게임 화면이 함께 스크롤되던 문제를 수정하고, 닫을 때 원래 페이지 위치를 복원합니다.

### Preserved

- 서버 API, Firestore 읽기·쓰기·인덱스, 상대 cursor 페이지와 배틀 판정은 변경하지 않았습니다.

## [0.5.7.0] - 2026-08-22

### Added

- Ghost 아레나 기록 탭에 5건 단위 `이전`·`다음` 페이지 탐색을 추가했습니다.

### Changed

- 전체·현재 연결 공격·Ghost 기록 필터가 각각 전용 cursor와 페이지 cache를 사용해 정확히 5건씩 표시합니다.
- 방문한 기록 페이지는 모달이 열린 동안 메모리에서 재사용하며, 새로고침은 현재 필터의 첫 페이지부터 다시 조회합니다.

### Preserved

- 전체 기록 수 집계와 문서 쓰기는 추가하지 않았으며 배틀 기록 저장·상세 보관 계약은 변경하지 않았습니다.

## [0.5.6.0] - 2026-08-22

### Added

- Ghost 아레나에 `내 Ghost` 독립 탭을 추가해 대전 화면은 현재 디지몬과 도전 상대에 집중하도록 정리했습니다.
- 도전 상대 제목에 현재 페이지와 전체 상대 수를 표시하고, 6명 단위 `이전`·`다음` 페이지 이동을 추가했습니다.

### Changed

- 방문한 상대 페이지는 모달이 열린 동안 메모리에 보관해 이전 페이지나 이미 확인한 페이지로 돌아갈 때 추가 조회하지 않습니다.
- 모바일 현재 디지몬 카드의 빈 공간에 최종 Power와 계산 요약을 배치하고 도전 상대 목록이 남은 높이를 더 넓게 사용하도록 개선했습니다.
- 전체 상대 수는 최초 조회와 수동 새로고침에서만 집계하며, 정렬 변경과 페이지 이동에서는 기존 집계값을 재사용합니다.

### Preserved

- Ghost 저장 계약, 전적 갱신, 배틀 판정과 상대 은닉 규칙은 변경하지 않았습니다.

## [0.5.5.0] - 2026-08-22

### Added

- Ghost 아레나를 `대전`과 `기록` 탭으로 나누고, 현재 디지몬·내 Ghost·도전 상대를 한 화면에서 빠르게 확인할 수 있는 대전 중심 화면을 추가했습니다.
- 도전 상대를 등록 최신순·오래된순과 Ghost 방어 승리 많은순·적은순으로 정렬할 수 있습니다.

### Changed

- 도전 상대를 처음 6명만 조회하고 `더보기`마다 다음 6명을 불러오도록 변경해 초기 Firestore 읽기와 화면 길이를 줄였습니다.
- 현재 디지몬과 내 Ghost 카드, 상대 목록, 배틀 기록을 압축하고 Power·규칙 상세는 별도 패널에서 확인하도록 정리했습니다.
- 개발환경에서는 별도 환경 변수 없이 Ghost 아레나 V2를 기본 표시하며, 명시적으로 `false`를 설정한 경우에만 기존 화면을 표시합니다.

### Fixed

- 정렬 변경·새로고침·연속 더보기 요청에서 목록이 중복되거나 이전 정렬 응답이 섞이지 않도록 요청과 cursor를 격리했습니다.

## [0.5.4.0] - 2026-08-20

### Fixed

- 등록 후 진화·사망·새 생애로 원본 연결이 끝난 Ghost에도 등록 당시 모습 그대로 정상 도전할 수 있습니다.
- 수면 스케줄이 없는 새 생애 슬롯을 이전 Ghost의 원본 손상으로 오인하던 판정 순서를 바로잡았습니다.
- 실제 원본 형태 손상은 기존처럼 차단하고, 일시적 projection 실패는 재시도 가능한 503 응답으로 구분합니다.

### Changed

- Ghost 원본 검증 실패 시 개인 식별자와 슬롯 내용을 제외한 구조화 경고를 남겨 운영 원인을 구분할 수 있게 했습니다.

## [0.5.3.0] - 2026-08-18

### Added

- 몰입형 세로 화면에 원작풍 10버튼 픽셀 스킨 3종을 추가했습니다.
- 스킨별로 왼쪽 배경, 오른쪽 배경, 중앙 게임기 색상을 조정하고 프리셋으로 복원할 수 있는 `외형 꾸미기`를 추가했습니다.

### Changed

- 10개 주요 액션을 기존 메뉴 순서와 잠금 규칙을 유지한 `5×2` 배치로 표시하고, 중앙 LCD에는 현재 `GameScreen`을 그대로 사용합니다.
- 외형 설정은 스킨별로 슬롯의 기존 `immersiveSettings`에 저장되며, 저장 전 취소하면 실제 게임 화면은 바뀌지 않습니다.

### Preserved

- 기본 `/play/:slotId` UI와 기존 가로 몰입형 UI는 변경하지 않았습니다.

## [0.5.2.0] - 2026-08-17

### Changed

- 모바일 세로 화면의 `디지몬 상태`와 `디지몬 상태 상세` 팝업이 상단부터 안전 영역을 제외한 화면 높이를 거의 전부 사용해 더 많은 상태 정보를 한 번에 볼 수 있습니다.
- 긴 상태 상세 내용은 본문만 스크롤되고 제목과 하단 닫기 버튼은 계속 보이도록 정리했습니다.

### Fixed

- 모바일 `호출 상태` 팝업의 좌우 여백을 약 8px로 줄여 긴 호출·케어미스 기록이 좁게 잘리거나 과도하게 줄바꿈되던 문제를 개선했습니다.

## [0.5.1.0] - 2026-08-15

### Added

- 신규 스탯 센터에 모든 사용자가 확인할 수 있는 `[위험]` 탭을 추가해 배고픔·힘·배변·부상 관련 사망 조건과 남은 시간을 한곳에서 확인할 수 있습니다.
- 상한 없는 누적 수명을 `일·시간·분·초`로 표시하고, 냉장고 보관이나 사망으로 시간이 멈춘 상태와 사망 원인을 함께 보여줍니다.

### Changed

- 사망 후 위험 카운터와 상태 칩을 현재 경고가 아닌 사망 당시 기록으로 표시하고, 모든 시간 계산을 저장된 사망 시각에 고정합니다.
- 운영자는 `[상태] [위험] [고급·진단]`, 일반 사용자는 `[상태] [위험]` 탭을 사용하며 기존 `[Old] [New]` 화면과 저장 동작은 그대로 유지합니다.

### Fixed

- 배변 8개 카운터와 이미 임계값을 지난 위험의 데드라인이 실제 기록과 다르거나 현재 시각에 따라 계속 이동하던 표시 오류를 수정했습니다.
- 사망 후 수면·조명·호출 같은 현재형 경고가 남아 보이거나 직접 사망 조건이 `사망 위험`으로 표시되던 문제를 수정했습니다.

## [0.5.0.0] - 2026-08-13

### Added

- 일반 진화가 네트워크 끊김으로 저장되지 못한 경우 기기에 내구성 있게 보존했다가 재연결 시 같은 진화로 재시도합니다.
- 로컬 조그레스를 인증된 서버 API에서 처리해 현재 슬롯 진화와 파트너 슬롯 사망을 하나의 요청으로 안전하게 완료합니다.

### Changed

- 슬롯·디지몬의 각 생애를 고유 identity로 구분해 슬롯 삭제·재생성·환생 후에 이전 생애의 대기 저장과 로그가 섞이지 않도록 했습니다.
- 새로운 시작·일반 진화의 형태, 스탯, 전투 identity, 활동 로그가 함께 저장되도록 저장 경계를 통합했습니다.
- 현재 생애의 활동 기록과 배틀 기록을 각각 최대 50개로 유지하고, 이전 생애의 서브컬렉션 기록을 현재 생애에서 제외합니다.

### Fixed

- 다른 기기·탭에서 슬롯이 먼저 변경됐을 때 오래된 진화·조그레스 요청이 최신 상태를 덮어쓰지 않도록 양쪽 revision과 생애 identity를 검증합니다.
- 로컬 조그레스 성공 직후에도 서버가 확정한 진화 로그가 활동 기록 화면에 중복 없이 바로 반영되도록 했습니다.

## [0.4.2.0] - 2026-08-12

### Added

- 기존 `[Old] [New]` 스탯 화면을 보존하면서, 일반 사용자에게는 `[상태]`, 운영자에게는 `[상태] [고급·진단]`을 제공하는 별도 스탯 센터를 추가했습니다.
- 운영자가 배고픔·힘·에너지·체중·배변·케어 및 배틀 카운터·부상 상태를 정해진 범위 안에서 수정할 수 있는 1차 편집 기능을 추가했습니다.

### Changed

- 인게임 스탯 패널의 문구를 한글화하고 아코디언 접근성 속성을 보강했습니다.
- 진단 화면의 현재 형태·이번 생애 누적 배틀 용어를 명확히 구분하고, 배변은 `배변 횟수 N/8`, 타이머는 `분·초`, 수명은 `일·시간·분·초` 형식으로 표시합니다.

## [0.4.1.0] - 2026-08-09

### Fixed

- 아이패드와 태블릿 화면에서 상단 메뉴가 겹치지 않고 브랜드·채팅·알림·계정 도구를 한 줄로 사용할 수 있도록 수정했습니다.
- 좁은 화면에서는 홈·플레이·커뮤니티·더보기 메뉴를 하단 탭바로 제공하고, 본문이 탭바에 가려지지 않도록 여백을 보정했습니다.

## [0.4.0.1] - 2026-08-09

### Fixed

- Ver.4·Ver.5 디지몬이 사망 확인 후 `🥚 새로운 시작` 버튼으로 새 생명을 시작할 수 있도록 수정했습니다.
- 사망 폼 판정을 버전별 설정과 공통 유틸리티로 통합해 버전별 후속 버튼이 누락되지 않도록 했습니다.

## [0.4.0.0] - 2026-08-09

### Changed

- 운영자 마스터 데이터 저장·복원을 인증된 서버 API와 Firestore transaction으로 이전했습니다.
- 마스터 데이터 변경 충돌과 네트워크 재시도를 revision·멱등성 snapshot으로 안전하게 처리합니다.
- 사용이 끝난 조그레스 로그·레거시 아레나와 모든 `game_settings`의 클라이언트 쓰기를 차단했습니다.
- Firestore Rules와 transaction Emulator 검사를 독립 CI 작업으로 추가했습니다.

### Fixed

- 잘못된 마스터 데이터 JSON과 revision 타입을 서버 경계에서 명확하게 거부하도록 수정했습니다.

## [0.3.9.0] - 2026-08-04

### Added

- 등록 당시 디지몬을 보존하는 1회용 조그레스 Ghost를 추가했습니다.
- 옛 형태의 방에는 참가자만 진화하고, 같은 슬롯의 새로운 형태는 별도 방으로 등록할 수 있습니다.

### Changed

- 온라인 조그레스의 생성·참가·완료·취소를 인증된 서버 API와 Firestore transaction으로 이전했습니다.
- 조그레스 방을 현재 형태의 `live`와 등록 형태 `ghost`로 구분해 표시합니다.
- 레거시 waiting·expired·paired 방을 안전하게 Ghost v3로 이전할 수 있는 백업·복원 마이그레이션을 추가했습니다.

## [0.3.8.0] - 2026-08-04

### Added

- Ver.3↔Ver.4, Ver.3↔Ver.5, Ver.4↔Ver.5 조합으로 온라인 조그레스를 진행할 수 있습니다.
- 온라인 조그레스에서 호스트와 게스트가 각자 버전의 결과 디지몬으로 진화하고 모두 생존합니다.

### Changed

- 방 참가 판정, 결과 이름·스냅숏·도감·로그 갱신을 Ver.1~Ver.5 공통 규칙으로 통일했습니다.
- 로컬 조그레스는 파트너 슬롯을 삭제하지 않고 `조그레스 파트너` 사망 상태로 보존함을 안내합니다.

## [0.3.7.2] - 2026-08-04

### Fixed

- 상단 홈 로고를 누르면 계정의 기본 화면 설정이 적용되는 루트(`/`)로 이동하도록 수정했습니다.

## [0.3.7.1] - 2026-08-04

### Fixed

- 기본 화면을 `플레이`로 설정해도 홈 메뉴와 홈 버튼에서 실제 홈 화면으로 이동하도록 수정했습니다.
- 기본 시작 경로(`/`)와 명시적 홈 경로(`/home`)를 분리했습니다.

## [0.3.7.0] - 2026-08-04

### Added

- 계정 설정에서 로그인 후 첫 화면을 `홈` 또는 `플레이`로 선택할 수 있습니다.
- 기본 화면이 `플레이`이면 로그인 직후와 루트 진입 시 플레이 허브로 이동합니다.

## [0.3.6.0] - 2026-08-03

### Changed

- 플레이 허브에서 `슬롯 순서 변경` 버튼을 `자세히`·`간략히` 왼쪽에서 바로 사용할 수 있도록 위치를 옮겼습니다.
- 기존 `슬롯 순서 정리` 버튼 문구를 `슬롯 순서 변경`으로 변경했습니다.

## [0.3.5.0] - 2026-08-03

### Changed

- Ver.5 디지타마·밀레니엄몬·카오스드라몬의 한글 이름에 버전 표기를 추가했습니다.
- Ver.5 진화 안내에도 `밀레니엄몬 [Ver.5]`과 `카오스드라몬 [Ver.5]`을 표시합니다.

## [0.3.4.0] - 2026-08-03

### Changed

- Ver.1~Ver.4 디지타마의 버전 표기를 통일하고, Ver.4·Ver.5 디지몬 한글 이름을 공식 표기에 맞게 보정했습니다.
- 진화 및 조그레스 안내에 표시되는 한글 이름도 실제 디지몬 이름과 일치하도록 정리했습니다.

## [0.3.3.0] - 2026-08-02

### Fixed

- 실시간 배틀을 X 또는 배경 클릭으로 종료한 뒤 다음 입장 시 이전 결과 팝업이 다시 나타나던 문제를 수정했습니다.
- 진행 중 배틀과 대기방을 닫을 때 종료 확인을 거치고, 종료 요청이 실패하면 현재 화면을 유지하도록 했습니다.

## [0.3.2.0] - 2026-08-02

### Changed

- 모바일 실시간 배틀 모달을 화면 상단부터 사용할 수 있고, 화면 아래 내용은 모달 내부에서 스크롤할 수 있습니다.
- 배틀 모달의 헤더와 닫기 버튼을 고정해 긴 화면에서도 모달을 닫기 쉽게 했습니다.

## [0.3.1.1] - 2026-08-02

### Fixed

- 실시간 배틀에서 아직 행동을 선택하지 않은 라운드의 복구·자동 판정 응답이 500 오류로 끝나던 문제를 수정했습니다.

## [0.3.1.0] - 2026-08-02

### Changed

- 실시간 배틀에서 내 선택과 CPU/상대 선택을 파란색과 보라색으로 구분하고, 최근 라운드의 행동과 받은 피해량을 참가자별 영역으로 나누어 표시합니다.
- 선택 제한시간을 행동 버튼 바로 위에 표시하고 3초 이하에서 빨간색으로 강조하며, 디지몬 카드에 단계·파워·속성 상성을 함께 표시합니다.
- 개발환경에서 실시간 배틀을 바로 테스트할 수 있도록 진입 버튼과 운영 API 프록시를 구성했습니다.

### Fixed

- 같은 라운드의 마감 판정 요청이 겹쳐도 완료된 결과를 멱등 재생하며, 신·구 운영 API 계약이 섞인 배포 중에도 0초 판정이 중단되지 않도록 하위 호환 요청을 적용합니다.
- 복구·마감 판정 요청 실패가 React 개발 오류 오버레이로 중복 노출되지 않도록 처리했습니다.

## [0.3.0.0] - 2026-08-02

### Added

- 실시간 배틀에서 양쪽 행동을 동시에 공개하고 공격 스프라이트, 중앙 방패, 피격 흔들림, 실제 피해량과 HP 변화를 순서대로 보여줍니다.
- 행동을 선택하지 않으면 공격·방어·특수공격 중 하나를 자동으로 골라 정상 판정하며, 자동 선택 여부를 라운드 요약에 표시합니다.

### Changed

- 7초 선택시간 동안 행동을 자유롭게 바꿀 수 있으며, 마지막으로 서버에 저장된 선택을 강조해 보여줍니다.
- 판정 연출이 끝난 뒤 다음 라운드의 7초 선택시간을 온전히 시작하고, 재접속 시 서버 시각을 기준으로 현재 선택·연출 단계를 복구합니다.

## [0.2.0.0] - 2026-08-01

### Added

- 실시간 배틀 로비에서 전적과 보상에 반영되지 않는 `CPU와 배틀` 연습전을 바로 시작할 수 있습니다.
- CPU 상대는 사용자 디지몬의 파워와 단계를 기준으로 자동 매칭되며, 배틀 시작과 함께 공개됩니다.
- CPU는 라운드와 체력 상태에 따라 공격·방어·필살기를 결정적으로 선택하며, 응답 유실과 재시도에도 같은 결과를 재생합니다.

### Changed

- CPU 배틀은 대기방 목록에 노출되지 않고, 배틀 화면에서 참가자를 `나`와 `CPU`로 표시합니다.
- CPU 연습전 결과는 `승리`, `패배`, `무승부`로 안내하며 육성 전적과 보상을 변경하지 않습니다.

## [0.1.2.0] - 2026-07-30

### Changed

- 실시간 배틀은 성장기 이상이면 서로 다른 성장 단계의 디지몬끼리도 참가하고 시작할 수 있습니다.
- 대기방은 Ghost 아레나처럼 테이머명만 표시하고 디지몬의 이름·단계·이미지는 `???`로 가립니다.
- 방을 만든 사람에게 방 만료까지 남은 시간을 표시합니다.
- 대기방이 없을 때 직접 방을 만들도록 안내하는 문구를 표시합니다.

## [0.1.1.0] - 2026-07-30

### Added

- 실시간 배틀 로비에서 현재 참가 가능한 대기방의 디지몬, 단계, 남은 시간을 확인하고 바로 참가할 수 있습니다.
- 기존 초대 방 ID 직접 입력 방식은 함께 유지됩니다.

### Fixed

- 푸니몬처럼 성장기 전 디지몬으로 방을 만들 때 내부 상태 확인 오류 대신 `성장기 이상의 디지몬만 실시간 배틀에 참가할 수 있습니다.`라고 정확히 안내합니다.
- 대기방 목록에서 만료된 방과 이미 참가자가 찬 방을 제외하고, 사용자 UID를 응답에 노출하지 않습니다.

## [0.1.0.0] - 2026-07-30

### Added

- 두 사용자가 방 ID를 공유해 같은 단계의 디지몬으로 7라운드 실시간 친선 배틀을 할 수 있습니다.
- 공격·방어·특수공격 선택, 7초 제한 시간, 재접속 복구, 타임아웃·포기 처리를 서버 권위 판정으로 추가했습니다.
- 첫 행동과 상대방의 미해결 행동을 비공개 문서에만 보관하고, 참가자만 공개 배틀 상태를 읽을 수 있도록 보안 규칙을 추가했습니다.

### Changed

- 실시간 배틀은 랭크·보상·육성 전적을 변경하지 않는 친선전으로 제공되며, feature flag와 서버 허용 목록으로 비공개 검증할 수 있습니다.

### Fixed

- 게스트가 방 참가에 성공하기 전 Firestore를 구독해 실시간 갱신이 끊길 수 있던 경합을 수정했습니다.
- 로비 명령 영수증을 UID에 묶어 취소·나가기·준비 응답이 유실되어도 다른 사용자와 충돌하지 않고 같은 결과를 재생하도록 수정했습니다.

## [0.0.0.2] - 2026-07-30

### Changed

- 진화 버튼이 초록색으로 활성화된 때에만 `진화!`를 표시하고, 진화할 수 없는 회색 상태에서는 `진화?`를 표시합니다.

## [0.0.0.1] - 2026-07-28

### Fixed

- Discord 일일 보고의 시간형 카운터 진행 바가 데드라인 문장에 이어 붙지 않고, 긴급 보고와 동일하게 다음 줄의 고정폭 블록으로 표시됩니다.
