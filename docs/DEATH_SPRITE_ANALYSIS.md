# 사망 스프라이트 분석 문서

## 📊 현재 구현 상태

### 사망 시 스프라이트 표시 로직

**위치**: `src/pages/Game.jsx` (726-737줄)

```javascript
// 죽음 상태: 모션 15번(아픔2) 사용, 스프라이트 2와 15 표시
if(digimonStats.isDead){
  // 모션 15번 (아픔2) - digimonAnimations[15] = battleLose (frames: [1, 14])
  // 스프라이트 2와 15를 번갈아 표시
  idleFrames= [ `${digimonStats.sprite+2}`, `${digimonStats.sprite+15}` ];
  eatFramesArr= [ `${digimonStats.sprite+2}`, `${digimonStats.sprite+15}` ];
  rejectFramesArr= [ `${digimonStats.sprite+2}`, `${digimonStats.sprite+15}` ];
  // 죽음 상태에서는 항상 아픔2 모션 사용
  if(currentAnimation !== "pain2"){
    setCurrentAnimation("pain2");
  }
}
```

### 애니메이션 정의

**위치**: `src/data/digimonAnimations.js`

```javascript
15: { name: "battleLose", frames: [1, 14] },
```

**의미**: 
- `frames: [1, 14]`는 `sprite+1`과 `sprite+14`를 의미함
- 즉, 기본 스프라이트에서 +1, +14 오프셋을 사용

---

## 🔍 발견된 불일치

### 문제점

1. **주석과 실제 코드 불일치**:
   - 주석: "모션 15번(아픔2) - digimonAnimations[15] = battleLose (frames: [1, 14])"
   - 실제 코드: `sprite+2`, `sprite+15` 사용
   - 애니메이션 정의: `frames: [1, 14]` → `sprite+1`, `sprite+14`를 의미

2. **애니메이션 정의와 실제 사용 불일치**:
   - 애니메이션 정의: `frames: [1, 14]` → `sprite+1`, `sprite+14`
   - 실제 사용: `sprite+2`, `sprite+15`

3. **레거시 코드와의 차이**:
   - 레거시 코드(`digimontama_1`): `sprite+15`만 사용 (단일 프레임)
   - 현재 코드: `sprite+2`, `sprite+15` 사용 (2프레임 애니메이션)

---

## 🤔 분석

### 가능한 시나리오

#### 시나리오 1: 애니메이션 정의가 올바름
- **올바른 스프라이트**: `sprite+1`, `sprite+14`
- **현재 코드**: `sprite+2`, `sprite+15` (잘못됨)
- **수정 필요**: `sprite+1`, `sprite+14`로 변경

#### 시나리오 2: 현재 코드가 올바름
- **올바른 스프라이트**: `sprite+2`, `sprite+15`
- **애니메이션 정의**: `frames: [1, 14]` (잘못됨)
- **수정 필요**: `frames: [2, 15]`로 변경

#### 시나리오 3: 의도된 다른 동작
- 사망 시 특별한 스프라이트 사용 (애니메이션 정의와 무관)
- 현재 구현이 의도된 것일 수 있음

---

## 📝 권장 사항

### 확인 필요 사항

1. **실제 스프라이트 파일 확인**:
   - `sprite+1`, `sprite+14` 파일이 존재하는지
   - `sprite+2`, `sprite+15` 파일이 존재하는지
   - 어떤 스프라이트가 사망 상태에 적합한지

2. **매뉴얼 확인**:
   - Digital Monster Color 매뉴얼에서 사망 시 스프라이트 규칙 확인
   - `battleLose` 애니메이션이 사망 시 사용되는지 확인

3. **테스트**:
   - 현재 구현(`sprite+2`, `sprite+15`)이 올바르게 표시되는지
   - 애니메이션 정의대로(`sprite+1`, `sprite+14`) 변경 시 올바르게 표시되는지

### 수정 완료 ✅

#### 옵션 1 적용: 애니메이션 정의에 맞춤 (2026-01-04)
```javascript
// 죽음 상태: 모션 15번(아픔2) 사용, 스프라이트 1과 14 표시
if(digimonStats.isDead){
  // 모션 15번 (아픔2) - digimonAnimations[15] = battleLose (frames: [1, 14])
  // 스프라이트 1과 14를 번갈아 표시 (애니메이션 정의에 맞춤)
  idleFrames= [ `${digimonStats.sprite+1}`, `${digimonStats.sprite+14}` ];
  eatFramesArr= [ `${digimonStats.sprite+1}`, `${digimonStats.sprite+14}` ];
  rejectFramesArr= [ `${digimonStats.sprite+1}`, `${digimonStats.sprite+14}` ];
  if(currentAnimation !== "pain2"){
    setCurrentAnimation("pain2");
  }
}
```

#### 옵션 2: 애니메이션 정의 수정
```javascript
// src/data/digimonAnimations.js
15: { name: "battleLose", frames: [2, 15] }, // 사망 시 사용
```

#### 옵션 3: 주석만 수정 (현재 구현 유지)
```javascript
// 죽음 상태: 스프라이트 2와 15를 번갈아 표시 (사망 전용)
if(digimonStats.isDead){
  // 사망 시 특별 스프라이트 사용 (battleLose 애니메이션과는 별개)
  idleFrames= [ `${digimonStats.sprite+2}`, `${digimonStats.sprite+15}` ];
  // ...
}
```

---

## 🎯 현재 동작 (수정 후)

### 표시되는 스프라이트
- **프레임 1**: `sprite+1` (예: sprite가 360이면 361.png)
- **프레임 2**: `sprite+14` (예: sprite가 360이면 374.png)
- 두 프레임이 번갈아가며 애니메이션됨
- **애니메이션 정의와 일치**: `digimonAnimations[15] = battleLose (frames: [1, 14])`

### 추가 시각 효과
- **GameScreen.jsx**: 양쪽에 해골 이모지(💀) 표시 (128-163줄)
- **애니메이션**: `pain2` 모션 사용

---

## 📚 관련 파일

- `src/pages/Game.jsx`: 사망 스프라이트 설정 (726-737줄)
- `src/components/Canvas.jsx`: 사망 애니메이션 렌더링 (84-86줄)
- `src/data/digimonAnimations.js`: 애니메이션 정의 (15번)
- `src/components/GameScreen.jsx`: 사망 시 해골 이모지 표시 (128-163줄)

