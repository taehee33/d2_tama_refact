# CareMistakes 증가 문제 분석 및 해결 방안

## 🔍 문제 분석

### 현재 상황
- CareMistakes가 계속 올라가는 문제 발생
- 매뉴얼과 다른 동작

### 근본 원인

#### 1. **중복 실행 문제** ⚠️
두 곳에서 careMistakes를 증가시키고 있음:

1. **`applyLazyUpdate` (stats.js 359-368줄)**
   - 배고픔/힘이 0일 때 호출 타임아웃 체크
   - 10분 경과 시 careMistakes +1
   - **추가 실수 계산** (방치시간 / (TimerCycle + 10분)) ❌ 매뉴얼에 없음

2. **`checkCallTimeouts` (useGameLogic.js 435-490줄)**
   - Game.jsx 400줄에서 호출
   - 10분 경과 시 careMistakes +1

**문제**: 둘 다 실행되면 중복으로 증가!

#### 2. **추가 실수 계산 로직** ❌
```javascript
// stats.js 363-368줄
// 추가 실수 계산: (방치시간) / (TimerCycle + 10분) 만큼 추가 실수
if (updatedStats.hungerTimer > 0) {
  const timerCycleMs = updatedStats.hungerTimer * 60 * 1000;
  const additionalMistakes = Math.floor(elapsed / (timerCycleMs + HUNGER_CALL_TIMEOUT));
  updatedStats.careMistakes += additionalMistakes;
}
```

**매뉴얼 규칙**:
- Call이 발생하면 10분(배고픔/힘) 또는 1시간(수면) 내에 응답해야 함
- 타임아웃되면 **careMistakes +1만** 증가
- 추가 실수 계산은 **없음**

#### 3. **진화 시 리셋** ✅
- `useEvolution.js` 166줄에서 `careMistakes: 0`으로 리셋 (정상)

---

## 📋 매뉴얼 규칙 (Digital Monster Color Manual)

### Care Mistakes 정의
1. **Call 발생 시**:
   - 배고픔이 비어있을 때
   - 힘이 비어있을 때
   - 수면 중 조명을 끄지 않았을 때

2. **타임아웃**:
   - 배고픔/힘: 10분 내 응답하지 않으면 careMistakes +1
   - 수면: 1시간 내 조명을 끄지 않으면 careMistakes +1

3. **중복 Call**:
   - 배고픔과 힘이 동시에 비어있으면 **2개의 Care Mistakes** 발생 가능
   - 각각 독립적으로 타임아웃 체크

4. **진화 시 리셋**:
   - 진화할 때마다 careMistakes는 0으로 리셋

5. **사망 조건**:
   - Stage IV, V: 진화 타이머 종료 시 5개 이상의 Care Mistakes → 사망
   - Stage VI, VI+: 48시간 후 5개 이상의 Care Mistakes → 사망

---

## 🔧 해결 방안

### 방안 1: `applyLazyUpdate`에서 추가 실수 계산 제거 (권장)
- `stats.js` 363-368줄의 추가 실수 계산 로직 제거
- 10분 타임아웃 시 +1만 증가하도록 수정

### 방안 2: 중복 실행 방지
- `applyLazyUpdate`에서 careMistakes 증가 로직 제거
- `checkCallTimeouts`만 사용하도록 통합

### 방안 3: 호출 리셋 로직 개선
- Call이 응답되면 즉시 리셋
- 타임아웃 후에도 중복 증가 방지

---

## ✅ 권장 수정 사항

1. **`stats.js` 수정**:
   - 추가 실수 계산 로직 제거 (363-368줄)
   - 10분 타임아웃 시 +1만 증가

2. **중복 실행 방지**:
   - `applyLazyUpdate`에서 careMistakes 증가 로직 제거
   - `checkCallTimeouts`만 사용

3. **호출 리셋 확인**:
   - Call 응답 시 즉시 `isActive = false` 처리 확인

---

## 📝 참고 사항

- 매뉴얼: "If an empty meter is not filled after the call light goes out on its own, your Digimon will call again when its timer for that heart meter runs out again, and can result in another care mistake."
- 즉, Call이 타임아웃되면 리셋되고, 다시 0이 되면 새로운 Call이 발생할 수 있음
- 하지만 한 번의 타임아웃당 +1만 증가해야 함


